import 'package:flutter/material.dart';

class GenerateFromText extends StatelessWidget {
  const GenerateFromText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text(""),
    );
  }
}
