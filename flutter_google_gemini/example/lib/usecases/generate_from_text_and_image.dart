import 'package:flutter/material.dart';

class GenerateFromTextAndImage extends StatelessWidget {
  const GenerateFromTextAndImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text(""),
    );
  }
}
