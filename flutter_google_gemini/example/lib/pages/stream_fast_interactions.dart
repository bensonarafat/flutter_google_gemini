import 'package:flutter/material.dart';

class StreamFastInteraction extends StatelessWidget {
  const StreamFastInteraction({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text("Stream Fast Interactions"),
      ),
      body: const Text(""),
    );
  }
}
