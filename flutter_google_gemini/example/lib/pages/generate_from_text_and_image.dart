import 'package:flutter/material.dart';

class GenerateFromTextAndImage extends StatelessWidget {
  const GenerateFromTextAndImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text("Genrate From Text and Image"),
      ),
      body: const Text(""),
    );
  }
}
